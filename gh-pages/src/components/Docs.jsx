import React from "react"; // eslint-disable-line no-unused-vars

/**
 * The view component is responsible for making requests and populating the datasource for the Map and Table child components
 */
const Docs = class Docs extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div className="jumbotron">
        <h1 className="text-center">Temp</h1>
        This component is temporary until the generated JSdocs are restored
      </div>
    );
  }
};

export default Docs;
