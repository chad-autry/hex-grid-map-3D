/**
 * A set of tests for the main java file
 */
//Require the Constructor of the object being tested
var ExampleForegroundContext = require("../exampleForegroundContext.js");

describe( 'ExampleForegroundContext dummy test', function() {

    it( 'it should have a test', function() {
        expect( true );
    });

});